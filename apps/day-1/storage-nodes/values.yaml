# Default values for storage-nodes.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
storageNodes:
  clusterSets:
    - hub
    - managed
  zones:
    - us-east-2c