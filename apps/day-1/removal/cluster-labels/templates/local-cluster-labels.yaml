apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: {{ .Values.hubCluster.name | default "local-cluster" }}
  labels:
    {{- range $index, $item := .Values.hubCluster.clusterlabels }}
    {{ $item.name }}: {{ $item.value | default "" }}
    {{- end }}