{{- $allClusterSets := (merge (dict) ($.Values.hubClusterSets | default dict) ($.Values.managedClusterSets | default dict)) }}
{{- $hubClusterSet := $.Values.hubClusterSet | default "hub" }}
{{- $policyName := (print "policy-cluster-labels" "-" ($.Values.deploymentName | default "default")) }}
{{- $placementName := printf "placement-%s" $policyName }}
{{- $policyNamespace := $.Values.autoshiftPolicyNamespace }}
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: {{ $policyName }}
  namespace: {{ $policyNamespace }}
  annotations:
    policy.open-cluster-management.io/categories: CM Configuration Management
    policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
    policy.open-cluster-management.io/standards: NIST SP 800-53
spec:
  disabled: false
  remediationAction: enforce
  policy-templates:
{{- range $clusterSetKey, $clusterSetValue := $allClusterSets }}
  {{- $configPolicyName := printf "cluster-labels-%s" $clusterSetKey }}
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: {{ $configPolicyName }}
          annotations:
        spec:
          severity: high
          object-templates-raw: |
            [
              {{ "{{-" }} $thisClusterSet := "{{ $clusterSetKey }}" {{ "}}" }}
              {{ "{{-" }} $configMapNamespace := "{{ $policyNamespace }}" {{ "}}" }}
              {{ "{{-" }} $clusters := (lookup "cluster.open-cluster-management.io/v1" "ManagedCluster" "" "").items {{ "}}" }}
              {{ "{{-" }} $lastIndex := (sub (len $clusters) 1) {{ "}}" }}
              {{ "{{-" }} range $index, $value := $clusters {{ "}}" }}
                {{ "{{-" }} $clusterSet := ((index $value.metadata.labels "cluster.open-cluster-management.io/clusterset") | default "") {{ "}}" }}
                {{ "{{-" }} if or (eq $clusterSet "") (ne $clusterSet $thisClusterSet) {{ "}}" }}
                  {{ "{{-" }} continue {{ "}}" }}
                {{ "{{-" }} end {{ "}}" }}
                {{ "{{-" }} $managedCluster := $value.metadata.name {{ "}}" }}
                {{ "{{-" }} $existingClusterLabels := $value.metadata.labels {{ "}}" }}
                {{ "{{-" }} $existingClusterLabelsNoAutoshift := dict {{ "}}" }}
                {{ "{{-" }} range $existingKey, $existingValue := $existingClusterLabels {{ "}}" }}
                  {{ "{{-" }} if not ($existingKey | hasPrefix "{{ $.Values.autoshiftLabelPrefix }}") {{ "}}" }}
                    {{ "{{-" }} $_ := set $existingClusterLabelsNoAutoshift $existingKey $existingValue {{ "}}" }}
                  {{ "{{-" }} end {{ "}}" }}
                {{ "{{-" }} end {{ "}}" }}
                {{ "{{/*" }} ////////// BEGIN LABEL EXTRACTION ////////// {{ "*/}}" }}
                  {{ "{{-" }} $clusterSetConfigMap := (lookup "v1" "ConfigMap" $configMapNamespace (printf "cluster-set.%s" $clusterSet)) | default dict {{ "}}" }}
                  {{ "{{-" }} $clusterSetConfigMapData := (index $clusterSetConfigMap "data") | default dict {{ "}}" }}
                  {{ "{{-" }} $clusterSetConfigMapValues := (index $clusterSetConfigMapData "values") | default "" {{ "}}" }}
                  {{ "{{-" }} $clusterSetLabels := (index ($clusterSetConfigMapValues | fromJson | default dict) "labels") | default dict {{ "}}" }}
                  {{ "{{-" }} $clusterConfigMap := (lookup "v1" "ConfigMap" $configMapNamespace (printf "managed-cluster.%s" $value.metadata.name)) | default dict {{ "}}" }}
                  {{ "{{-" }} $clusterConfigMapData := (index $clusterConfigMap "data") | default dict {{ "}}" }}
                  {{ "{{-" }} $clusterConfigMapValues := (index $clusterConfigMapData "values") | default "" {{ "}}" }}
                  {{ "{{-" }} $clusterLabels := (index ($clusterConfigMapValues | fromJson | default dict) "labels") | default dict {{ "}}" }}
                {{ "{{/*" }}  ////////// END LABEL EXTRACTION ////////// {{ "*/}}" }}
                {{ "{{-" }} $configMapLabels := merge (dict) $clusterLabels $clusterSetLabels {{ "}}" }}
                {{ "{{-" }} $labels := merge (dict) $configMapLabels $existingClusterLabelsNoAutoshift {{ "}}" }}
                {{ "{{-" }} $newLabels := dict {{ "}}" }}
                {{ "{{-" }} range $label, $labelValue := $labels {{ "}}" }}
                  {{ "{{-" }} if not (and ($label | hasPrefix "{{ $.Values.autoshiftLabelPrefix }}") (eq $labelValue "_")) {{ "}}" }}
                    {{ "{{-" }} $_ := set $newLabels $label $labelValue {{ "}}" }}
                  {{ "{{-" }} end {{ "}}" }}
                {{ "{{-" }} end {{ "}}" }}
                {{ "{{-" }} $metadata := set $value.metadata "labels" $newLabels {{ "}}" }}
                {{ "{{-" }} $objectDefinition := set $value "metadata" $metadata {{ "}}" }}
                {{ "{{-" }} $_ := unset $objectDefinition "status" {{ "}}" }}
                {
                  "complianceType": "mustonlyhave",
                  "objectDefinition": {{ "{{" }} $objectDefinition | toRawJson  {{ "}}" }}
                }{{ "{{-" }} if ne $lastIndex $index {{ "}}" }},{{ "{{-" }} end {{ "}}" }}
              {{ "{{-" }} end {{ "}}" }}
            ]
{{- end }}
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: {{ $placementName }}
  namespace: {{ $policyNamespace }}
spec:
  clusterSets:
    - {{ $.Values.selfManagedHubSet | default "hubofhubs" }}
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: {{ $placementName }}
  namespace: {{ $policyNamespace }}
placementRef:
  name: {{ $placementName}}
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: {{ $policyName }}
    apiGroup: policy.open-cluster-management.io
    kind: Policy
---
{{/* $clusterSetLabels := (index ((index (index ((lookup "v1" "ConfigMap" "{{ $policyNamespace }}" (printf "cluster-set.%s"  $clusterSet)) | default dict) "data" | default dict) "values" | default "" | fromJson) | default dict) "labels") | default dict */}}
{{/* $clusterLabels := (index ((index (index ((lookup "v1" "ConfigMap" "{{ $policyNamespace }}" (printf "managed-cluster.%s"  $value.metadata.name)) | default dict) "data" | default dict) "values" | default "" | fromJson) | default dict) "labels") | default dict */}}
