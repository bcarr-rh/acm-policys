# Default values for worker-nodes.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
autoshiftPolicyNamespace: open-cluster-policies
machinehealthchecks:
  zones:
    - us-east-2c